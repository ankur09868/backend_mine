from django.apps import AppConfig

class WalletConfig(AppConfig):
    name = 'wallet'  # Change this to match the name of your wallet app
    verbose_name = 'Wallet Management'  # Optional: A human-readable name for the app
